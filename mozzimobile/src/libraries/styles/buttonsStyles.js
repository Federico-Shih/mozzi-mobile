import { StyleSheet } from 'react-native';
import { fontType } from './constants';

export default StyleSheet.create({
  reglogButtonText: {
    fontSize: 20,
    fontFamily: fontType,
  },

  reglogButton: {
    paddingVertical: 12,
  },
});
